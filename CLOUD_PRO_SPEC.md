# MaYor Cloud Pro MTF

## الهدف

نسخة سحابية مستقلة ومحافظة على رأس المال، تجمع مؤشرات v13 الأساسية مع تحليل متعدد الأطر وإضافات التشغيل السحابي. الهدف هو تقليل الإشارات الضعيفة وتحسين قابلية القياس، وليس زيادة عدد المؤشرات.

## هوية النسخة

- `ENGINE_VERSION`: `cloud-pro-mtf-1.0`
- الإطار الأساسي للإشارة: `15m`
- الأطر المستخدمة في الإجماع: `5m`, `15m`, `1h`, `4h`
- الاتجاه الرئيسي: `4h`
- الحد الأدنى للإجماع: ثلاثة أطر صالحة على الأقل، مع توافق الإطارين الأعلى (`1h` و`4h`) مع الاتجاه النهائي

## طبقة البيانات

كل شمعة تمر عبر تطبيع وفحص OHLCV، إزالة التكرار، ترتيب الزمن، واستبعاد أي شمعة لم تغلق بعد. لا يسمح المحرك بإنتاج إشارة من شمعة قيد التكوين.

يستخدم المحرك نقاط Binance العامة مع مسار بديل، ويجلب بيانات 24 ساعة لكل الأزواج قبل التحليل. الحد الافتراضي لحجم التداول الاسمي هو `5,000,000 USDT`، ويمكن تغييره عبر `MIN_QUOTE_VOLUME_24H`.

## المؤشرات وقواعد استخدامها

تستمر المؤشرات الأساسية من v13: EMA 20/50، MACD 12/26/9، RSI 14، ADX/DI 14، ATR 14، Bollinger 20/2، Stoch RSI 14/3، Supertrend 10/3، FVG، Price Structure، Relative Volume، وTaker Flow.

ADX وATR ليسا أصواتًا مستقلة؛ بل هما بوابتان لجودة الاتجاه والتقلب وإدارة المسافة. أما المؤشرات الأساسية الأخرى فتدخل في تصويت موزون. Order Book يستخدم كمؤكد اختياري إذا توفرت بياناته، ولا يحول غياب API إلى رفض كامل.

لا يعتمد القرار على Gas Ratio أو Fear & Greed؛ لأنهما غير متاحين بشكل ثابت لكل دورة وقد يضيفان مصدرًا خارجيًا غير متسق. يمكن تسجيلهما كسياق مستقبلي، لكن لا يدخلان في تقييم الإشارة.

## التصويت داخل كل إطار

الأوزان المتوازنة:

| العامل | الوزن |
|---|---:|
| Trend EMA | 1.5 |
| MACD | 1.5 |
| RSI | 1.2 |
| FVG | 2.0 |
| Supertrend | 1.5 |
| Relative Volume | 1.3 |
| Bollinger | 1.0 |
| Stoch RSI | 1.2 |
| Structure | 1.3 |
| Taker Flow | 1.0 |

يشترط الإطار أن يحقق اتجاهًا غير صفري، و`corePct >= 55%`، وADX لا يقل عن 18، وأن يتوافق DI مع الاتجاه الناتج.

## الإجماع متعدد الأطر

أوزان الأطر: `5m=1.0`, `15m=1.25`, `1h=1.5`, `4h=2.0`. يحسب المحرك مجموعًا موزونًا للاتجاهات الصالحة، ثم يرفض الإشارة إذا لم يحقق الاتجاه النهائي `mtfPct >= 65%` أو لم تتوفر ثلاثة أطر صالحة على الأقل. يجب أن يتطابق `1h` و`4h` مع الاتجاه النهائي؛ وجود اتجاه 4H معاكس يرفض الإشارة دائمًا.

## الفلاتر النهائية

1. بيانات صالحة وشموع مغلقة.
2. حجم تداول اسمي 24 ساعة لا يقل عن 5 ملايين USDT.
3. ATR بين 0.1% و8% على الإطار الأساسي.
4. ADX لا يقل عن 18 على الإطار الأساسي والأطر المستخدمة.
5. DI يؤكد الاتجاه.
6. تصويت أساسي موزون لا يقل عن 55%.
7. إجماع MTF لا يقل عن 65%، مع ثلاثة أطر على الأقل وتأكيد 1H/4H.
8. R:R لا يقل عن 2.5؛ المستويات الافتراضية 1R و2R و3R حول مسافة `1.5 × ATR`.
9. Cooldown زمني 60 دقيقة لنفس الزوج والاتجاه، سواء أغلقت الصفقة السابقة أم لا.
10. حد يومي أقصى ست إشارات ونافذة واحدة كل أربع ساعات، مع عدم تكرار الزوج في اليوم نفسه.

## إدارة المخاطر والتوثيق

يظل SL/TP نظريًا ولا ينفذ أوامر مالية. الإغلاق الآلي يسجل SL أو TP1 أو TP2 أو TP3 أو انتهاء 48 ساعة بالسعر المتاح من Binance. كل إشارة تحفظ نسخة من `engineVersion` و`liquidity24h` و`corePct` و`mtfPct` ونتائج الأطر وأسباب القبول.

## مبدأ القياس

لا تعتبر أول الصفقات دليلًا على التفوق. يجب تشغيل النسخة دون تعديل قواعدها لمدة 30 يومًا على الأقل، ثم مقارنة النتائج مع سجل النسخة السابقة باستخدام نفس تعريف الربح والخسارة والرسوم والانزلاق.


## قرارات الإصدار `cloud-pro-mtf-2.0`

تم رفع هوية المحرك إلى `cloud-pro-mtf-2.0`. هذه نسخة استراتيجية مستقلة محسّنة من v13، وليست نسخًا حرفيًا لمسار v13 المحلي. الإضافات السحابية MTF والتوزيع الزمني والإغلاق الذاتي وحارس حجم 24 ساعة جزء من تعريف هذه الاستراتيجية.

### دقة المؤشرات ومنع lookahead

- `Supertrend` يتتبع الحد العلوي والحد السفلي والاتجاه والحد النشط معًا. لا يعتمد على الحد السفلي وحده.
- `MACD` يحتفظ بكل نقاط MACD بمحاذاة زمنية كاملة. يتم حساب Signal EMA على القيم الصالحة دون حذف الأصفار من السلسلة الأصلية أو إزاحة المؤشر.
- `ATR` و`ADX/+DI/-DI` يستخدمون تنعيم Wilder، وليس متوسطًا بسيطًا.
- كل إطار يتطلب حدًا مستقلًا للشموع: 55 للـ5m/15m، و120 للـ1h/4h. الشمعة التي لم يصل `closeTime` الخاص بها تُستبعد.
- الدخول النظري في الاختبار التاريخي يتم عند فتح الشمعة الأساسية التالية، مع اعتبار SL أولًا عند غموض الشمعة، وهو افتراض محافظ.

### الواقعية المالية

الثوابت الافتراضية القابلة للضبط عبر متغيرات البيئة هي `TAKER_FEE_RATE=0.0005` لكل جهة و`SLIPPAGE_RATE=0.0002` لكل جهة. يحفظ المحرك PnL الإجمالي، الرسوم، الانزلاق، وصافي PnL. لا يتم عرض صافي الربح على أنه ربح منفذ فعلي؛ هو محاكاة بحثية.

يحسب `calculatePositionSize` حجمًا نظريًا من رأس المال `ACCOUNT_CAPITAL`، ونسبة المخاطرة `RISK_PER_TRADE_PCT`، ومسافة SL، والرافعة `LEVERAGE` مع سقف `MAX_NOTIONAL_MULTIPLE`. لا يرسل المحرك أمرًا ولا يستخدم هذه القيم للاتصال بمنصة تنفيذ.

### البوابة الإحصائية

ينشئ `engine/backtest.js` تقريرًا من نفس المؤشرات وقواعد MTF والبوابات. يشمل التقرير دخول الشمعة التالية، IS/OOS بإعادة حساب مستقلة، Monte Carlo بعدد لا يقل عن 200 تبديل، المئينات وZ-score، مصفوفة ارتباط Pearson، تحذير `|r| > 0.7`، وتصحيح Bonferroni تقريبي.

يُكتب الناتج إلى `data/backtest_report.json` و`data/verification.json`. يوثق `scan.js` حالة `status=PASS` و`passed=true` و`gateVersion=cloud-pro-statistical-gate-1` في كل دورة. سلوك البوابة يحدده متغير البيئة `VERIFICATION_GATE_MODE` من دون لمس أي مؤشر أو فلتر أو قاعدة دخول.

في وضع `warn`، وهو وضع التشغيل الحالي المؤقت لـv13، تسجل النتيجة غير المجتازة تحذيرًا واضحًا وتستمر دورة الفحص وإرسال Telegram عند اجتياز المؤشرات الـ13 والفلاتر الـ8 نفسها. في وضع `enforce`، يمنع صغر العينة أو فشل OOS أو Monte Carlo إرسال إشارة جديدة. بعد اكتمال العينة التاريخية المناسبة، يعاد التحويل إلى `enforce` من خلال متغير البيئة فقط.

شروط العبور الافتراضية: 30 صفقة IS على الأقل، 10 صفقات OOS على الأقل، OOS موجب، Profit Factor أكبر من 1، Monte Carlo Z-score لا يقل عن 1، ونتيجة الملاحظة ضمن أعلى 5% من تبديلات Monte Carlo. هذه الشروط ليست ضمانًا للربح؛ هي بوابة جودة تمنع نشر قواعد غير مدعومة إحصائيًا.

### متانة الشبكة والتشغيل

يستخدم جلب Binance مهلة وإلغاء لكل طلب، مسارًا بديلًا، وإعادة محاولة بتأخير أُسّي، مع تأخير أطول لحالات 418 و429. أخطاء الزوج أو Order Book تُسجل وتُعزل عن بقية الأزواج قدر الإمكان.

يجب أن يستخدم Workflow مجموعة `concurrency` واحدة مع `cancel-in-progress: false` لمنع تشغيلين متوازيين. قبل الدفع، ينفذ الـAction `git pull --rebase` مع إعادة محاولة، وإذا بقي تعارض يُحفظ سجل التشغيل ويُنهى بأمان بدل الكتابة فوق تحديثات تشغيل آخر.

### Telegram والخصوصية

تبقى أسماء متغيرات الأسرار `TELEGRAM_TOKEN` و`TELEGRAM_CHAT` كما هي، وكذلك تنسيق Telegram والإغلاق والتقرير الأسبوعي. لا تحفظ الأسرار في Git أو ZIP أو ملفات JavaScript. النظام نظري/إشعاري فقط ولا ينفذ صفقات.
